"use client";

interface ExperienceEffectProps {
  // Add any props if needed in the future
}

export function ExperienceEffect({ /* props */ }: ExperienceEffectProps) {
  return (
    <div className="experience-effect-grid absolute inset-0">
      {/* This div will contain the grid pattern */}
    </div>
  );
}
